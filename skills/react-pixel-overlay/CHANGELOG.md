# Changelog

## 1.0.0

- Initial release
- Setup guide for Vite + React apps: install, `public/design-overlays/` folder, `pixelOverlaySources` Vite plugin, gated mount pattern
- Props and keyboard-shortcut reference
- Gotchas: React dedupe for file:/git installs, natural-size rendering, eslint import resolver, non-Vite usage, localStorage quota
- Typical pixel-QA workflow including difference blend mode and pre-seeding overlay state for agent-driven screenshots
